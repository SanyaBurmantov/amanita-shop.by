import React, {FC} from 'react';

interface Button{

}

export const Button:FC<Button> = ({}) => {
    return (
        <></>
        // <button type='submit' className='button' onClick={addNewReviews}>Отправить
        // </button>
    );
};
